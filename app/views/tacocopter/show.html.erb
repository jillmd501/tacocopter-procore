<h1 style="text-align:center">Eat up!</h1>

<table style="width:100%">
  <tr>
    <th>Store Name</th>
    <th>Location</th> 
  </tr>
  <tr>
    <td>
    	<%= @stores.each do |store| %>
          <%= store.name %>
         <% end %>
    </td>
    <td>
    	<%= @cities.each do |city| %>
    	  <% city.name %>
    	<% end %>
    </td>
  </tr>
</table>